<style type="text/css">
    article{
        width: 150px;
        padding: 10px;
        border: 1px solid white;
        display: inline-block;
        background: #ccc;
        margin: 10px
    }

    #sugerencia{
        background: gray;
        text-align: center;
        padding: 10px
    }

    .titulo{
        font-size: 20px;
        font-weight: bold;
    }

</style>



<div class="row-fluid">
    <div class="span10 offset1" id="sugerencia">
        <h4>Por que ha visto</h4>

        <?
        $peliculas = ($this->parametros['peliculas']);
//var_dump($peliculas);
        $HTML = "";

        foreach ($peliculas as $key => $value) {
            $k = $value->getData();
            $HTML.='<article>
                <p class="titulo">'
                    . $k['titulo'] .
                    '<p>
                <img src="' . $k['imagen'] . 'height="150" width="150">' .
                    '<p>'
                    . $k['categoria'] .
                    '<p>
            </article>';

            echo $HTML;
            $HTML = "";
        }
        ?>

    </div>
</div>

<div class="espFooter">
</div>
<div class="row-fluid">
    <div class="span10 offset1" id="sugerencia">
        <h4>Se le recomienda ver</h4>
        <?
        $sugerencias = $this->parametros['sugerencias'];

        foreach ($sugerencias as $key => $value) {
            $k = $value->getData();
            $HTML.='<article>
                <p class="titulo">'
                    . $k['titulo'] .
                    '</p>
                <img src="' . $k['imagen'] . 'height="150" width="150">' .
                    '<p>'
                    . $k['categoria'] .
                    '<p>
            </article>';

            echo $HTML;
            $HTML = "";
        }
        ?>
    </div>
</div><div class="espFooter">
</div>