<?

$peliculas = ($this->parametros['peliculas']);
//var_dump($peliculas);
$HTML = "";

foreach ($peliculas as $key => $value) {
    $k = $value->getData();
    $HTML.='<article>
    <h2>';
    $HTML.= $k['titulo'] .
            '</h2>';
    $HTML.='<img src="';
    $HTML.= $k['imagen'];
    $HTML.= 'height="150" width="150">';
    $HTML.='<h3>' . $k['categoria'] . '</h3>';
    $HTML.="</article>";
    
    echo $HTML;
    $HTML ="";
}
?>

<div class="espFooter">
</div>